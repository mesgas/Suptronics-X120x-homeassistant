# Text files keep LF endings in the repository whatever the contributor's
# system: without this rule a commit made from Windows would rewrite every line
# of every file and the diffs would become unreadable.
* text=auto eol=lf

*.png binary
